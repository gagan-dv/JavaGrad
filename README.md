# JavaGrad

A tiny automatic gradient engine built from scratch in Java.
Every number is wrapped in an `AutoGrad` object. It tracks every math operation done to it and automatically computes gradients by calling `backward()`.
